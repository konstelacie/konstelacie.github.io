<!-- Generic funnel content: CTA, how, audience, pricing, FAQ, references, CTA again -->
<section id="booking" class="funnel-cta booking-section prose"
  <% if (typeof funnelName !== 'undefined' && funnelName) { %>data-funnel-name="<%= funnelName %>"<% } %>
  <% if (typeof funnelName !== 'undefined' && funnelName && typeof funnelCampaignId !== 'undefined') { %>data-funnel-campaign="<%= funnelCampaignId %>"<% } %>
  <% if (typeof funnelVideoId !== 'undefined' && funnelVideoId) { %>data-funnel-video-id="<%= funnelVideoId %>"<% } %>>
  <div id="booking-main" class="booking-main">
  <div class="container">
    <h2>Vyber si termín</h2>
    <p>Vyber si termín, ktorý Ti vyhovuje.</p>
    <p class="muted">Krátko podržíme termín, kým zadáš e-mail; na dokončenie platby potom máš 15 minút.</p>

    <section class="booking-calendar" aria-label="Rezervácia termínu">
      <p id="booking-calendar-loading" class="booking-calendar__loading muted">Načítavam termíny…</p>
      <div id="booking-calendar-inner" class="booking-calendar__inner" hidden>
        <div id="booking-calendar-hero" class="booking-calendar__hero" hidden>
          <p class="booking-calendar__hero-label">Najbližší voľný termín:</p>
          <div id="booking-hero-slot-host" class="booking-calendar__hero-slot-host" role="group" aria-label="Najbližší voľný termín"></div>
        </div>
        <div id="booking-calendar-days" class="booking-calendar__days"></div>
      </div>
      <p id="booking-slots-empty" class="booking-calendar__empty muted" hidden>
        Momentálne nie sú dostupné žiadne termíny.
      </p>
      <p id="booking-slots-error" class="booking-error booking-calendar__error" hidden></p>
    </section>

    <div id="booking-hold-banner" class="booking-hold-banner" hidden>
      <p id="booking-hold-message"><strong>Termín je rezervovaný.</strong> Dokonči platbu do 15 minút.</p>
      <p id="booking-countdown" class="booking-countdown" aria-live="polite">15:00</p>
      <button type="button" id="booking-revoke-btn" class="booking-revoke-btn">Zrušiť podržanie</button>
    </div>

    <div id="booking-payment-choice" class="booking-payment-choice" hidden>
      <p class="booking-payment-intro">Môžeš si zvoliť sumu, ktorá ti vyhovuje.</p>
      <form id="booking-payment-form" class="booking-payment-form">
        <fieldset class="booking-payment-options">
          <legend class="visually-hidden">Spôsob platby</legend>
          <label class="booking-payment-option">
            <input type="radio" name="paymentPath" value="deposit" id="payment-deposit">
            <span class="booking-payment-option-content">
              <strong>Rezervovať sedenie</strong>
              <span class="booking-payment-desc">Zaplať teraz malý poplatok za rezerváciu a finálnu sumu rozhodneš po sedení.</span>
              <span class="booking-payment-amount">Rezervovať za 10 €</span>
            </span>
          </label>
          <label class="booking-payment-option">
            <input type="radio" name="paymentPath" value="full" id="payment-full">
            <span class="booking-payment-option-content">
              <strong>Zaplatiť plnú sumu teraz</strong>
              <span class="booking-payment-desc">Minimálna suma: 45 €. Odporúčaná suma: 85 €.</span>
              <div class="booking-payment-amounts">
                <label class="booking-amount-opt"><input type="radio" name="fullAmount" value="45"> 45 € <span class="muted">(znížená suma)</span></label>
                <label class="booking-amount-opt"><input type="radio" name="fullAmount" value="65"> 65 €</label>
                <label class="booking-amount-opt booking-amount-recommended"><input type="radio" name="fullAmount" value="85" checked> 85 € <span class="muted">(odporúčaná suma)</span></label>
                <label class="booking-amount-opt"><input type="radio" name="fullAmount" value="105"> 105 € <span class="muted">(podporujúca suma)</span></label>
                <label class="booking-amount-opt">
                  <input type="radio" name="fullAmount" value="custom" id="full-amount-custom"> Vlastná suma:
                  <input type="number" id="booking-custom-amount" min="45" step="1" value="125" class="booking-custom-amount-input" aria-label="Vlastná suma v eurách"> €
                </label>
              </div>
            </span>
          </label>
        </fieldset>
        <p id="booking-payment-error" class="booking-error" hidden></p>
        <div class="booking-payment-actions">
          <button type="button" id="booking-payment-back" class="booking-back-btn">Späť na e-mail</button>
          <button type="submit" id="booking-payment-submit" class="cta">Pokračovať k platbe</button>
        </div>
      </form>
    </div>

    <div id="booking-success" class="booking-success" hidden>
      <h3>Rezervácia je vytvorená</h3>
      <p id="booking-success-pending" class="muted">Presmerovávam na platbu…</p>
      <p id="booking-success-failed" class="booking-error" hidden></p>
      <button type="button" id="booking-payment-retry" class="cta" hidden>Skúsiť platbu znova</button>
    </div>
  </div>
  </div>

  <div id="booking-email-modal" class="booking-modal" hidden>
    <div class="booking-modal__backdrop" data-booking-modal-dismiss tabindex="-1" aria-hidden="true"></div>
    <div
      class="booking-modal__dialog"
      role="dialog"
      aria-modal="true"
      aria-labelledby="booking-modal-title"
    >
      <button type="button" id="booking-modal-close" class="booking-modal__close" aria-label="Zavrieť">×</button>
      <h3 id="booking-modal-title" class="booking-modal__title">Ešte jeden krok a termín máš rezervovaný</h3>
      <p id="booking-modal-phase" class="booking-modal__phase">
        Tento termín pre teba držíme, kým zadáš email.
      </p>
      <p id="booking-modal-countdown" class="booking-modal__countdown" aria-live="polite">05:00</p>
      <form id="booking-email-form" class="booking-email-form booking-modal__form">
        <label for="booking-email">E-mail</label>
        <input type="email" id="booking-email" name="email" required placeholder="vas@email.sk" autocomplete="email">
        <p id="booking-email-error" class="booking-error" hidden></p>
        <button type="submit" id="booking-submit-btn" class="cta">Pokračovať</button>
      </form>
      <button type="button" id="booking-modal-revoke" class="booking-modal__revoke">Zrušiť podržanie</button>
    </div>
  </div>
</section>

<details class="funnel-secondary">
  <summary class="funnel-secondary-trigger container">Ešte nie som rozhodnutý</summary>
  <div class="funnel-secondary-inner">
    <section class="funnel-how prose">
      <div class="container">
        <h2>Ako vyzerá sedenie</h2>
        <p>Placeholder popis priebehu sedenia…</p>
      </div>
    </section>

    <section class="funnel-audience prose">
      <div class="container">
        <h2>Pre koho to je / nie je</h2>
        <p>Placeholder text pre cieľovú skupinu…</p>
      </div>
    </section>

    <section class="funnel-pricing prose">
      <div class="container">
        <h2>Cena a platba</h2>
        <p>Rezervácia sedenia: 10 €. Plná suma: od 45 € (odporúčaná 85 €). Žiadne skryté poplatky.</p>
      </div>
    </section>

    <section class="funnel-faq prose">
      <div class="container">
        <h2>Časté otázky</h2>
        <dl class="funnel-faq-list">
          <dt>Pre koho je sedenie vhodné?</dt>
          <dd>Placeholder odpoveď…</dd>
          <dt>Pre koho sedenie vhodné ni je?</dt>
          <dd>Placeholder odpoveď…</dd>
          <dt>Ako sedenie prebieha?</dt>
          <dd>Vždy kladiem dôraz na vypočutie klienta, uznanie jeho prežívania a empatiu. Dôležité sú individuálne potreby klienta, nie presná štruktúra. Ak navnímam, že je vhodné prejsť ku konštelácii, navrhnem túto možnosť. Nikdy však ku konštelácii nepristupujem nasilu.</dd>
          <dt>Koľko to stojí?</dt>
          <dd>Prvá rezervácia stojí 10 €. V prípade nespokojnosti nemusíš nič ďalšie platiť a naše cesty sa môžu v pokoji rozísť. V prípade spokojnosti budeš môcť doplatiť sumu, akú si zvolíš. (Odporúčaná cena za sedenie je 85 €, minimálna 45 €).</dd>
          <dt>Čo sú rodinné konstelácie?</dt>
          <dd>Placeholder odpoveď…</dd>
        </dl>
      </div>
    </section>

    <section class="funnel-references prose">
      <div class="container">
        <h2>Odkazy a zdroje</h2>
        <p>Placeholder – knihy, články, odkazy na ďalšie informácie…</p>
      </div>
    </section>

    <section class="funnel-cta-repeat prose">
      <div class="container">
        <p><a href="#booking" class="cta">Vyber si termín</a></p>
      </div>
    </section>
  </div>
</details>
