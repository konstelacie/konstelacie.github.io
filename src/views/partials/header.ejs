<header class="site-header">
  <div class="container">
    <span class="brand">citimtedasom.sk</span>
    <nav class="nav">
      <% if (typeof home === 'undefined' || !home) { %>
        <%- include('site-back-link') %>
      <% } %>
    </nav>
  </div>
</header>
